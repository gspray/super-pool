# Server
PORT=3010

# Shared secret — must match the x-api-key on the ESP32
API_KEY=change-me-to-something-secret

# How often (ms) the NAS polls each ESP for status (default 15 000)
POLL_INTERVAL_MS=15000

# Path to persistent JSON data directory
DATA_DIR=./data
