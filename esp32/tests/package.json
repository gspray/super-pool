{
  "name": "esp32-super-pool-tests",
  "version": "1.0.0",
  "description": "Integration tests for the ESP32 super-pool controller",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:verbose": "jest --runInBand --verbose"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 15000,
    "testMatch": ["**/*.test.js"],
    "bail": 1
  }
}
